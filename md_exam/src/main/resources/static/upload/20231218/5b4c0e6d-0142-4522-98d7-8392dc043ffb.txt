작업순 : html -> db 생성 -> 객체(dto), controller -> (service) -> mappers

-ajax
주소를 직접입력하는건 Getmapping

-demo
Controller에서 Model model을 사용해서 HTML에 데이터를 넘겨준다
해당 HTML은 model의 name를 변수명으로 사용되어지네
그리고 [[${변수명}]] 으로 사용할수 있으면 출력은 value값

-tut00
Controller에서 @RequestMapping("/web")경우 공통된 주소를 뽑아내줌

-tut01
a태그에 href는 Controller의 GetMapping에 연결함

-tut02
버튼1 클릭시 ajax로 Controller의 데이터 받아오기 -> js로 ajax작성함 ->js 에서 받는 데이터를 innerText나 
innerHTML로 화면보여줌, 받을때는 ajax안에서 success:함수로 받네

버튼2 클릭시 ajax로 Controller에 데이터 넘겨줌 -> 여기서는 obj 오브젝트를 만들어서 data : obj 로 컨트롤러에
보내준다 obj안에 userid, passwd 두개가 있는데 RequestPram으로 Controller에서 받음

ajax 통신하려면 컨트롤러에 @ResponseBody 사용해야함

-tut03 
실행 직후 cannot resolve method >> lombok 플러그인 설치 ,, ;
form 전송하면 postmapping으로 주소에 연결하고 
Controller에서 객체를 생성하네? -> ModelAttribute RegDto
lombok을 써서 그런가 RegDto에는 필드만 있네
form에서 input name이 RegDto의 필드 이름이 동일하면 객체로 저장되네 

form 전송 ( ajax X )

챗집 : Spring MVC에서 폼 데이터를 받아오는 경우, 입력 폼의 name 속성과 DTO 객체의 필드(변수)명이 일치하면 자동으로 데이터가 매핑되어 DTO 객체에 저장됩니다. 이는 Spring의 데이터 바인딩 메커니즘에 의한 것입니다.


-tut04
버튼 이벤트에 ajax를 사용해서 전송
ajax에서 obj의 이름이 Dto와 연결되네
for in 사용






